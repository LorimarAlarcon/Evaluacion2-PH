import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion2.ph',
  appName: 'evaluacion2-ph',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
